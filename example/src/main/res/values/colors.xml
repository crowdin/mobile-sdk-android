<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Matches the Compose `surface` role, so there is no flash before the first frame. -->
    <color name="window_background">#F7FBF4</color>

</resources>
