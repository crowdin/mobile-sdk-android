<resources>

    <!--
      The UI is built with Jetpack Compose, so the platform theme only needs to keep the
      AppCompat/Material plumbing (locale handling, BaseContextWrappingDelegate) and stay out
      of the way. All colors come from the Compose color scheme.
    -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

</resources>
